#include <stdio.h>
/**
 * main - print all single digit of base ten
 *
 * Return: always (0);
 *
 */
int main(void)
{
	int	q;

	for	(q	=	0;	q	<	10;	q++)
	{
		printf("%d",	q);
	}
	putchar('\n');
	return	(0);
}
